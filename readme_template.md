# 实验名称 / 分支名称

> **分支说明**：基于 {main} 分支派生，将 Time-LLM 与 [论文/方法名称] 的核心机制结合，旨在提升时间序列预测精度。

---

## 1. 实验概览与元信息

- **实验编号/标签**：`E{编号}_{方法简写}`（如 `E01_WaveletPatch`）
- **日期**：`{YYYY-MM-DD}`
- **实验状态**：`{进行中 / 已完成 / 提升-保留 / 效果不佳-淘汰}`
- **基座 LLM**：GPT2
- **目标数据集**：ETTh1（默认输入长度 `seq_len={512}`）, 
- **训练脚本**：`scripts/ETTh1.sh`
- **训练日志**：`logs/{dataset}/{dataset}_{seq_len}_{model_comment}_{YYYY-mm-yy_HH:mm}.log`
- **参考论文/来源**：`{论文名 / GitHub 链接 / reference/ 对应目录}`

---

## 2. 为什么要做这次修改？（动机与设计思想）

### 2.1 原版 Time-LLM 的局限/痛点
- *说明原版代码或机制存在什么不足（例如：单一尺度 Patch 划分丢失高频突变特征、Prompt 文本统计量表达能力有限、Reprogramming 注意力机制对周期性建模不充分等）。*

### 2.2 引入的理论/模块与预期机理
- *说明本次引入的模块原理及其预期效果（例如：结合小波多尺度分解提取局部细节、引入频域掩码增强全局周期性表征、改进跨模态对齐损失等）。*

---

## 3. 修改了哪些文件的哪些模块？（代码变更清单）

| 修改文件 | 涉及类 / 函数 / 模块 | 改动类型 | 具体改动说明 | 对应原版实现 / 来源 |
| :--- | :--- | :--- | :--- | :--- |
| `layers/Embed.py` | `MultiScalePatchEmbedding` | **新增 / 替换** | 替换原版 `PatchEmbedding`，增加多尺度卷积与高低频分离逻辑 | 参考 `reference/xxx/` |
| `models/TimeLLM.py` | `Model.__init__` & `forecast` | **修改接口** | 适配新 Embedding 层的输出通道与维度，调整数据流拼接 | 原 `PatchEmbedding` 接口 |
| `run_main.py` | `ArgumentParser` | **新增参数** | 增加 `--wavelet_levels` 等控制新模块的超参数开关 | 新增实验超参 |
| `scripts/ETTh1.sh` | 启动命令 | **配置调整** | 注入新参数配置与 `model_comment` 标识 | 适配训练运行 |

---

## 4. 预测精度效果（对比基线）

> **评测口径**：输入长度 $L=512$，预测步长 $H \in \{96, 192, 336, 720\}$。记录验证集最优 MSE 对应轮次的测试集指标（MSE 与 同轮 MAE）。

### 4.1 核心指标对比表

| 预测步长 (pred_len) | 基线 MSE (GPT2) | 基线 MAE | 本方案 MSE | 本方案 MAE | MSE 变化率 ($\Delta$) | MAE 变化率 ($\Delta$) | 最佳轮次 (Epoch) | 训练速度 (it/s) |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **96** | 0.3840 | 0.4124 | | | % | % | | |
| **192** | 0.4296 | 0.4416 | | | % | % | | |
| **336** | 0.4730 | 0.4723 | | | % | % | | |
| **720** | 0.4501 | 0.4703 | | | % | % | | |
| **Avg (平均)** | **0.4342** | **0.4491** | | | **%** | **%** | - | |

> 注：$\Delta = \frac{\text{本方案} - \text{基线}}{\text{基线}} \times 100\%$（负值表示误差降低，即精度提升）。

### 4.2 实验结论与分析
- **整体效果**：*平均 MSE / MAE 相比基线是否有明显收益？*
- **长短步长差异**：*在短步长（如 96）还是长步长（如 720）上提升更为显著？*
- **收敛与开销**：*最佳轮次提前还是滞后？训练速度（it/s）与显存占用相比基线变化如何？*

---

## 5. 复现与运行指南

### 5.1 环境确认
```bash
conda activate time-llm
# 确认环境为 Python 3.11.11，GPU 为 RTX 4090D (24GB)
```

### 5.2 运行命令
```bash
# 运行当前分支专属实验脚本
bash scripts/ETTh1.sh
```

### 5.3 关键参数说明
- `--model_comment`：`{例如: gpt2_wavelet_patch}`
- `{新增的关键参数名}`：`{参数值说明}`
